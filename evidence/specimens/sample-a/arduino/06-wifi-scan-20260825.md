# Sample A / Arduino / 06_WiFiTest / Wi-Fi scan-only evidence

Status: `WIFI RADIO SCAN PHYSICAL PASS CANDIDATE / SAMPLE A / INFRASTRUCTURE PENDING`.

Date: `2026-08-25`

Video evidence:

```text
https://youtube.com/shorts/DOus0uNBBZI
```

## Test target

```text
Example      : 06_WiFiTest
Purpose      : Wi-Fi radio scan + optional infrastructure validation
Board target : ESP32-8048S043 Lab N16R8 FIXED (ESP32-S3 RGB800x480 GT911)
Profile      : local Arduino sketchbook hardware profile
Mode         : scan-only, no wifi_secrets.h present
```

## Observed serial evidence

Boot and test banner:

```text
ESP-ROM:esp32s3-20210327
rst:0x1 (POWERON),boot:0x8 (SPI_FAST_FLASH_BOOT)

============================================================
 ESP32-8048S043 Lab / 06_WiFiTest
 Wi-Fi radio + scan + optional infrastructure validation
============================================================
Lineage: adapted from WT32-SC01-PLUS Lab 08_WiFiTest pattern
```

Local credentials state:

```text
[INFO] Local secrets header: not present (scan-only mode)
```

Non-fatal startup note:

```text
[E][STA.cpp:556] disconnect(): STA not started! You must call begin first.
```

This message appeared before scan and did not prevent Wi-Fi STA mode, MAC readout or active scan.

STA identity:

```text
[INFO] STA MAC: 84:FC:E6:6C:69:3C
```

Active scan result:

```text
[SCAN] Starting active Wi-Fi scan...
[PASS] Wi-Fi scan completed: 3 network(s) found
  01  RSSI= -29 dBm  CH=13  AUTH=3  SSID=TECNO CAMON 50
  02  RSSI= -32 dBm  CH= 1  AUTH=3  SSID=ASUS_38_2G
  03  RSSI= -91 dBm  CH= 6  AUTH=3  SSID= [hidden]
```

Final scan-only result:

```text
WIFI RADIO / SCAN PHYSICAL PASS CANDIDATE
Full association/DHCP/DNS/TCP/reconnect validation: PENDING
```

## Result

```text
Wi-Fi STA mode start      : PASS candidate
STA MAC readout           : PASS
Active Wi-Fi scan         : PASS
Networks found            : PASS, 3 network(s)
Infrastructure association: PENDING, no wifi_secrets.h present
DHCP                      : PENDING
DNS                       : PENDING
TCP/HTTP                  : PENDING
Reconnect cycles          : PENDING
```

## Boundary

This evidence supports a Wi-Fi radio scan PASS candidate only.

It does not prove:

```text
association to a router;
DHCP lease;
DNS resolution;
TCP/HTTP connectivity;
reconnect stability;
Web setup;
GitHub OTA;
Widget Runtime.
```

Full Wi-Fi PASS requires a later run with local `wifi_secrets.h` and successful association, DHCP, DNS, TCP/HTTP and reconnect cycles.
